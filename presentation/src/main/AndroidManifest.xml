<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bristot.presentation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".provider.application.Dagger2Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.bristot.presentation.ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".provider.preferences.PreferencesProvider"
            android:authorities=".provider.cache.CacheProvider"
            android:exported="false" />

        <provider
            android:name=".provider.StethoProvider"
            android:authorities="com.facebook.stetho.StethoProvider"
            android:exported="false" />
    </application>

</manifest>